export enum AttractionReportStatusTypes {
  OPEN = "open",
  STOPPED = "stopped",
  CLOSED = "closed",
  CONFIRMED = "confirmed",
}

export enum AttractionReportTypes {
  ZREPORT = "zreport",
  XREPORT = "xreport",
}
