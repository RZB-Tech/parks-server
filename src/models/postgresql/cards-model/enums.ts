export enum CardStatusTypes {
  ACTIVE = "active",
  INACTIVE = "inactive",
  BLOCKED = "blocked",
  LOST = "lost",
  FROZEN = "frozen",
  RETURNED = "returned",
}

export enum CardType {
  CLASSIC = "classic",
  VIP = "vip",
  ORGANIZATION = "organization",
  VIRTUAL = "virtual",
}
