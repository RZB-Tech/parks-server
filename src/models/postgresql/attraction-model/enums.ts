export enum AttractionStatusTypes {
  ACTIVE = "active",
  INACTIVE = "inactive",
  STOPPED = "stopped",
  MAINTENANCE = "maintenance",
  CLOSED = "closed",
}

export enum AttractionReportTypes {
  ZREPORT = "zreport",
  XREPORT = "xreport",
}