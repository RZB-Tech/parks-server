export enum CashboxStatusTypes {
  ACTIVE = "active",
  INACTIVE = "inactive",
  STOPPED = "stopped",
  MAINTENANCE = "maintenance",
  CLOSED = "closed",
}

export enum CashboxTypes {
  PHYSICAL = "physical",
  VIRTUAL = "virtual",
}
